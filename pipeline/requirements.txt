# Pinned to the local conda env `sdo` so CI reproduces local output byte-for-byte.
# NOTE: PyPI's latest sunpy is 8.x -- the 7.1.2 pin is DELIBERATE (the dome
# pipeline this was ported from is validated against 7.1.2, and sunpy 8 changed
# coordinate-frame internals that frames_orient.py asserts on).
numpy==2.4.6
scipy==1.17.1
astropy==7.2.0
sunpy==7.1.2
sunkit-magex==1.1.0
streamtracer==2.6.0
astroquery==0.4.11
requests>=2.32,<3
# sunpy.map's FITS/WCS path pulls these in; listed explicitly so a slim CI
# image cannot resolve a surprise version.
matplotlib==3.10.9
# texture/: reproject does the Helioprojective -> Carrington resampling and
# pillow decodes/encodes the JPEG.  reproject is NOT optional -- GenericMap
# .reproject_to() raises without it, and its default roundtrip_coords=True is
# what keeps the far side from aliasing onto the near side.
reproject==0.19.0
pillow==12.2.0
